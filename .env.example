# Copiar a .env.local. Sin prefijo NEXT_PUBLIC_ a propósito: solo el proceso de
# Next llama a la API, así que el navegador no necesita —ni debe— saber dónde
# está el backend.
API_BASE_URL=http://localhost:8080
